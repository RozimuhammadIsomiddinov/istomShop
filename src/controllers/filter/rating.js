/*const Product = require("../../data/models/product");

const ratingFilter = async (req, res) => {
  let arr = [];
  try {
    const product = await Product.findAll();

    product.
  } catch (err) {
    res.status(400).json({
      message: "Error from ratingFilter controller",
      error: err.message,
    });
  }
};

module.exports = ratingFilter;
*/
